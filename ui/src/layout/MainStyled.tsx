// material-ui
import { styled } from "@mui/material/styles";

// assets

// styles

const MainStyleFunc = () => {
  return {
    borderBottomLeftRadius: 0,
    borderBottomRightRadius: 0,
    
  };
};

const MainStyled = styled("main")(MainStyleFunc);

export default MainStyled;
